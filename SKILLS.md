# VMotors Backend Skills

Use these Codex skills for repeatable VMotors backend workflows.

| Skill | Purpose |
|---|---|
| `backend-migration` | Continue the NestJS backend migration to VMotors while preserving the current NestJS architecture. |
| `vehicle-logic` | Review vehicle GraphQL, DTO, Schema, enum, filter and naming consistency. |

Each skill lives at `skills/<skill-name>/SKILL.md`.